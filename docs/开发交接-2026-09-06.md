# 开发交接（2026-09-06）

> 用户因额度不足要求暂停开发、保存当前进度并推送。本提交是开发快照，尚未完成生产验收。请先读本文，再继续实现或部署。README 与部署指南中描述了目标用法，不代表以下待办已经验证完成。

## 需求与提交位置

- 保留现有 Vercel + Neon 部署，增加全部使用 Cloudflare 资源的部署方式：Workers、D1、R2、Workers Assets。
- 增加 Linux VPS 一行命令安装及 Docker Compose 部署。
- 管理后台一键下载整站 ZIP，在新站后台导入并覆盖全部账号、密码、资料、文章、活动、报名、站点设置和上传图片。
- 用户要求提交说明使用中文，GitHub 身份使用 **Uniseem**。
- 本次交接分支：`codex/deployment-backup-handoff`，基于 `main` 的 `aa9be14`。暂不合并生产分支，不执行生产恢复或远端资源创建。
- `aa9be14`（修复玩家自动滚动并在手动操作后继续播放）已在本轮开始时推送到 main。
- 当前快照新增功能未完成 Vercel / Cloudflare / VPS 线上部署验证。

## 已保存的实现

### 跨平台基础

- Next.js / `@next/env` / ESLint Next 配置更新到 16.3.4，以满足 OpenNext 1.20.6 兼容要求；Prisma 与 PG/D1 adapter 使用 7.9.1。
- 新增 OpenNext、Wrangler、Cloudflare 类型和 fflate ZIP 依赖；锁文件已更新。
- `db:generate` 和 `postinstall` 同时生成 PostgreSQL、D1 客户端。
- `next.config.ts` 在 `BUILD_STANDALONE=1` 时生成 standalone；仅 D1 开发模式初始化 OpenNext 本地绑定。
- `SITE_URL` 作为运行时公开网址，优先于旧 `NEXT_PUBLIC_SITE_URL`；用于 OAuth 与页面元数据。
- 新增 `/api/health`，检查数据库及初始化表可用性，不返回内部连接信息。
- OAuth 请求域名校验适配 HTTPS 反向代理，始终和配置的公开域名比较；新增两项测试验证代理与伪造头边界。

### Cloudflare

- 配置：`wrangler.jsonc`、`open-next.config.ts`、`.dev.vars.example`、`public/_headers`。
- 模型与迁移：`prisma/cloudflare/`。英雄数组以 JSON 存储；上传媒体使用 R2，D1 保存引用。
- 核心实现：`src/lib/cloudflare.ts`、`database-provider.ts`、`prisma.ts`、`d1-atomic.ts`、`asset-storage.ts`、`database-transfer.ts`、`oauth/d1-accounts.ts`。
- 已为首次管理员注册、普通注册、资料审核、报名容量审核、OAuth 账号写入、活动日期同步增加 D1 原子操作分支。
- D1 不支持 Prisma 交互事务：通用客户端明确拒绝 `$transaction`，关键业务使用原生 D1 batch；不能用多次独立写入绕过。
- `scripts/cloudflare.mjs` 提供构建、预览、部署，并清空 OpenNext 自动复制的环境配置，避免把本地 Vercel 凭证打进包。
- 当前没有 Cloudflare Cron handler；活动在页面读取时按上海日期同步，无人访问时不会单独触发任务。
- 指南：`docs/Cloudflare部署.md`。

### VPS

- `Dockerfile`：Node 22 多阶段构建、迁移镜像、非 root standalone 运行镜像。
- `compose.yml`：PostgreSQL 17 持久卷、健康检查、迁移顺序、网站、每分钟日期同步、可选 Caddy HTTPS。
- `scripts/install.sh`：下载代码、生成密钥、保留现有配置与卷、更新失败中止，支持域名/目录/端口/自定义仓库。
- `deploy/` 包含配置模板、Caddy、scheduler、安装模拟测试和最终镜像 standalone 烟测脚本。
- 安装脚本已防止终端同名环境变量覆盖经校验的 Compose 配置。
- 指南：`docs/VPS部署.md`。脚本需要目标机已有 Docker Engine、Compose v2、git、curl、openssl，不自动安装系统组件。

### 后台备份与恢复（仍需重点收尾）

- 新增 `/admin/backup` 页面、导航入口、`BackupManager`、`/api/admin/backup`。
- 新增 `backup-format.ts`、`backup-service.ts`、`backup-zip.ts`。
- PostgreSQL 新迁移 `20260906030000_site_backup` 增加 `BackupTransfer` / `BackupChunk`，D1 模型同步。
- ZIP 格式为 `data.json` 元数据加 `media/<id>.bin`；元数据上限 8 MiB、媒体总量 128 MiB、单图 2 MiB、最多 5,000 张图片及 50,000 条记录。
- HTTP 按 384 KiB 原始数据分块，避开 Vercel 单次请求大小限制；服务端逐张处理媒体。
- 原头像 data URL 在备份时提取为媒体，恢复后的资料指向站内图片接口。
- PostgreSQL 使用一致性快照与事务覆盖；D1 先写新的不可变 R2 对象，再 batch 原子切换全部记录与图片引用。
- ZIP 保存密码哈希与 OAuth 绑定；源站加密配置先解密，恢复时用目标站的 `OAUTH_ENCRYPTION_KEY` 重新加密。
- 不迁移登录会话与 OAuth 临时 state，恢复后所有人重新登录；目标站域名和环境变量保留。
- 外链图片保留原地址，不下载外链；ZIP 不包含代码、数据库连接密码和宿主环境配置。
- ZIP 含敏感账号信息和 OAuth 应用密钥，不应公开提交或分享。

## 验证记录

| 检查 | 本轮结果 |
| --- | --- |
| 原有 `npm test` | 33 项通过 |
| 新 OAuth 代理域名测试 | 2 项通过 |
| PostgreSQL 首位管理员集成测试 | 3 项通过 |
| PostgreSQL OAuth 集成测试 | 4 项通过 |
| PostgreSQL 文章集成测试 | 1 项通过 |
| PostgreSQL 更新提醒集成测试 | 缓存改为以检查结果中的 ISO 时间为准，并停止用空 `upsert` 回读设置，避免 DateTime 列或 Prisma Dev 时区把缓存判成过期 |
| D1 原子操作集成测试 | 6 项通过，使用独立本地 D1/R2 |
| 实际 workerd 中 Prisma D1 CRUD | 9 项通过，包括日期、JSON、关联、DbNull、租约、文章和 R2 |
| VPS Bash / scheduler / Compose | 语法、模拟安装更新、配置与失败中止检查通过 |
| Docker 最终容器构建 | 本机无 Docker，仍需在 Linux 环境实装 |
| Next / standalone 构建 | 已通过；D1 客户端改为按需加载 |
| OpenNext 打包 | 本机 Windows 在复制 traced files 时 `symlink` 返回 EPERM，需 WSL 或 Linux |
| ZIP 备份协议与测试 | 单测、PostgreSQL 集成和 D1 集成（含 R2 恢复）均已通过 |

## 已在本轮收尾

1. **备份协议与容量边界。** API、客户端和 ZIP 解析器共用媒体分离格式。分块上限改为按 8 MiB 元数据 + 最多 5,000 张小图计算（`BACKUP_MAX_CHUNKS`），清单请求上限 2 MiB，单块 Base64 上限与 384 KiB 对齐。
2. **真实外键与审计字段。** 只强制 `userId` / `authorId` / `createdById` / `reviewedById` 以及报名对应活动；`updatedById` / `uploadedById` 允许指向已删账号。
3. **备份测试和说明。** 已补文档与单测；集成测试用 `BACKUP_TEST_DATABASE_URL` 的独立临时 schema，覆盖大于 4.5 MB ZIP、跨密钥、旧密码、目标账号消失、会话清除、图片、损坏文件、失败回滚和权限/Origin。
4. **构建入口。** 普通 Next 构建不再静态打包 D1 wasm。
5. **更新提醒缓存。** 业务上以缓存载荷里的 UTC ISO 时间为准；`getUpdateSettings` 改为查找后创建。并发检查在租约释放后会再读一次缓存，避免后到的请求把同一次检查再打一遍 GitHub。

## 仍建议在隔离环境完成

1. 在具备 Docker 的 Linux 环境实际跑 Compose 新装和更新；验证迁移、HTTPS、管理员初始化、代理后的 OAuth 和备份恢复。
2. 在隔离站点用浏览器走一遍后台 ZIP 下载、导入、确认和恢复。不要覆盖当前真实本地数据或生产数据。
3. 按需要把本分支合并到生产并发布。

## 本地环境与数据保护

- 工作目录：`C:\Users\fyc12\Documents\ChatGPT\sjtu-ow`，PowerShell，Node 22+。
- 当前 `.env.local` 含真实远端配置；不要打印内容、提交它，或让测试默认读取生产数据库。
- 本地网站原端口 3100；依赖已升级，恢复开发时可能需要重启旧 Next 进程。
- 原本地 Prisma Dev：主端口 55400，数据库 55401，shadow 55402。**这是现有网站数据，不用于覆盖恢复测试。**
- root 新建的专用集成实例：`ow-integration-root`，主端口 55510，数据库 55511，shadow 55512；运行会话曾为 69551。后续检查端口是否仍运行，不要假定会话 ID 长期有效。
- 备份协作计划另起 55610 / 55611 / 55612 独立实例；是否创建以实际进程为准。
- 原开发服务器与数据库不因交接主动关闭；测试进程可按端口、用途确认后停止。
- Prisma Dev/PGlite 代理的并行会话可能共享 `search_path`。四套测试并行曾互相干扰临时 schema；之后在专用实例逐套执行，管理员/OAuth/文章通过。应串行或每套使用独立实例，不能据该并发失败判断真实 PostgreSQL 有问题。
- 核查时原 public 数据仍有 3 个用户；文章 `05800f4f-9034-4cf3-a2c2-c66685e60e2f`（标题 `123`）仍在。活动 `cmto1ju1d0001vsja63ao1org` 已有用户设置的封面，**不要清空或还原**。
- 测试数据库 URL 显式指向本地独立实例；PG 测试自行创建和清理临时 schema，不能运行 reset 或针对原站 seed。
- 用户浏览器 `/me#public-profile` 是其当前页面；如需 UI 验证，另开隔离 QA 页面。

## 继续工作的入口

```bash
git switch codex/finish-backup-and-deploy
npm ci
npm test
npm run test:backup
```

集成测试使用独立 `*_TEST_DATABASE_URL`，不要指向原站 Prisma Dev（55400）或生产库。构建前按 AGENTS.md 读取当前 `node_modules/next/dist/docs/` 对应指南。

VPS 一行安装默认拉取 `main`。在合并本分支之前，测试安装需同时指定仓库 `--ref`。
